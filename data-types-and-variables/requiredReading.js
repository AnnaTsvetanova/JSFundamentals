function requiredReading (pages, norm, days) {

    let reqHours = pages / norm;
    let hoursPerDay = reqHours / days;

    console.log(hoursPerDay); 



}
//requiredReading(432, 15, 4);
requiredReading(212, 20 ,2);